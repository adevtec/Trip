/**
 * Data index file
 * Exports all data from the data directory
 */

// Export mock data
export * from './mock';

// In the future, this file could export real API data instead of mock data
// For now, we're just re-exporting the mock data
